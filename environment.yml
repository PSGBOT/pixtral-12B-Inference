
name: pixtral-inference
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - google-genai
    - mistralai
    - opencv-python
    - tqdm
    - Pillow
    - numpy
    - pydantic
    - setuptools
